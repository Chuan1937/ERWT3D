#!/bin/bash
# ERWT3D - 委托到 build/erwt3d
# 直接使用 ./build/erwt3d <command> key=value ...
# 或 ./build/erwt3d config.txt

BINDIR="$(cd "$(dirname "$0")" && pwd)/build"
BIN="$BINDIR/erwt3d"

if [ ! -x "$BIN" ]; then
    echo "错误：未找到 $BIN，请先执行 cmake --build build" >&2
    exit 1
fi

exec "$BIN" "$@"
